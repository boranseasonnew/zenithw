// Docker Compose serves the API through the same public origin.
window.ZENITHW_API = "/api";
// Self-hosters use their own network/IP and keep the normal YouTube flow.
window.ZENITHW_YOUTUBE_WEB_ENABLED = true;
